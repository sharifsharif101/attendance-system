# 🔒 برومت فحص الأخطاء الكارثية للنظام

## كيفية الاستخدام
انسخ البرومت أدناه واستخدمه مع أي AI Assistant (Claude, Gemini, ChatGPT)

---

## البرومت الكامل:

```
أنت خبير أمني متخصص في تدقيق أنظمة Laravel. مهمتك فحص هذا النظام بعمق للكشف عن:

## 🔴 المستوى 1: أخطاء كارثية (Data Loss / Security Breach)

### 1. فحص Foreign Keys
- ابحث عن `cascadeOnDelete` في جميع الـ migrations
- حدد أي cascade يمكن أن يسبب حذف بيانات مهمة
- اقترح تغييرها إلى `restrict` أو `nullOnDelete`

### 2. فحص Soft Deletes
- تحقق من أن Models الحرجة (User, Employee, Department) تستخدم SoftDeletes
- تحقق من وجود عمود `deleted_at` في الجداول

### 3. فحص Authorization
- ابحث عن Controllers بدون middleware للصلاحيات
- ابحث عن routes بدون حماية
- تحقق من أن `$request->user()->can()` موجود قبل العمليات الحساسة

### 4. فحص Mass Assignment
- تحقق من أن `$fillable` محدد في كل Model
- تأكد من عدم وجود `$guarded = []` 

### 5. فحص SQL Injection
- ابحث عن استخدام `DB::raw()` أو `whereRaw()` بدون binding
- تحقق من أن جميع المدخلات تمر عبر Eloquent أو prepared statements

## 🟡 المستوى 2: أخطاء منطقية (Business Logic Errors)

### 6. فحص Race Conditions
- ابحث عن عمليات read-then-write بدون locks
- تحقق من استخدام `lockForUpdate()` في العمليات المالية/الحرجة

### 7. فحص Validation
- تحقق من وجود validation rules لكل form request
- ابحث عن controllers تقبل input بدون validation

### 8. فحص Error Handling
- تحقق من وجود try-catch حول العمليات الخارجية
- تأكد من عدم عرض stack traces للمستخدم في production

## 🟢 المستوى 3: تحسينات أمنية (Security Hardening)

### 9. فحص Authentication
- تحقق من وجود rate limiting على login
- ابحث عن إمكانية تفعيل 2FA
- تحقق من قوة password requirements

### 10. فحص Session Security
- تحقق من إعدادات session في config/session.php
- تأكد من `secure` و `http_only` cookies

### 11. فحص CSRF
- تحقق من أن جميع forms تستخدم @csrf
- تأكد من عدم وجود routes POST بدون CSRF

### 12. فحص File Upload
- إذا وجد رفع ملفات، تحقق من:
  - تحديد الامتدادات المسموحة
  - تحديد الحجم الأقصى
  - تخزين الملفات خارج public

---

## 📋 شكل التقرير المطلوب

لكل مشكلة، قدم:

### 🔴 [اسم المشكلة]
**الملف:** `path/to/file.php:line_number`
**الخطورة:** كارثية / عالية / متوسطة / منخفضة
**الوصف:** شرح المشكلة
**السيناريو الكارثي:** ماذا يحدث لو استُغلت؟
**الحل:** الكود المطلوب للإصلاح

---

## 🎯 الأولويات

1. ابدأ بالأخطاء الكارثية أولاً
2. ركز على الملفات: Controllers, Models, Migrations
3. تجاهل vendor و node_modules
4. قدم حلول كود جاهزة للتنفيذ

---

ابدأ الفحص الآن وقدم تقريراً مفصلاً.
```

---

## 🚀 برومت مختصر للاستخدام السريع:

```
افحص هذا المشروع Laravel بحثاً عن:
1. cascadeOnDelete خطرة في migrations
2. Controllers بدون authorization
3. Models بدون SoftDeletes
4. SQL injection vulnerabilities
5. Missing validation
6. Race conditions

قدم تقريراً بـ: الملف، السطر، المشكلة، الحل الجاهز.
```

---

## 📝 ملاحظات:
- استخدم هذا البرومت بعد كل تعديل كبير على النظام
- احفظ التقارير للمراجعة المستقبلية
- نفذ الإصلاحات حسب الأولوية (الكارثية أولاً)
